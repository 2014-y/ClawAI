const { fork } = require('child_process'); const child = fork('node_modules/openclaw/dist/index.js', ['channels', 'login', '--channel', 'openclaw-weixin'], { execPath: '.\\.node-sandbox\\node.exe', stdio: 'pipe', env: { ...process.env, FORCE_COLOR: '1' } }); child.stdout.on('data', d => console.log('STDOUT:', d.toString())); child.stderr.on('data', d => console.log('STDERR:', d.toString())); child.on('exit', c => console.log('EXIT:', c));
